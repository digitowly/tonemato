schema: ${GRAPHQL_API}
documents: ./src/**/*.graphql
generates:
  ./src/generated/codegen_types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
