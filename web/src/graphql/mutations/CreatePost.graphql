mutation CreatePost(
  $title: String!
  $body: String!
  $author: UserCreateNestedOneWithoutPostsInput!
) {
  createPost(data: { title: $title, body: $body, author: $author }) {
    title
    body
  }
}
