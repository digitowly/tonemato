mutation CreateMusicianPost(
    $title: String!
    $body: String!
    $authorId: Float!
) {
    createMusicianPost(title: $title, body: $body, authorId: $authorId) {
        id
        title
        body
        author {
            id
            email
        }
    }
}
