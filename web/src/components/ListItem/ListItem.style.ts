import styled from 'styled-components';

export const ListItemWrapper = styled.li`
  display: flex;
  font-weight: bold;
  max-width: 150px;
  padding: 0.5rem 0;

  svg:first-child {
    margin-right: 1.5rem;
  }
`;
